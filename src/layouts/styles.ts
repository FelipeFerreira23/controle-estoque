import styled from "styled-components";

export const Layout = styled.div`
  min-height: 100vh;

  .content {
    margin: 0 auto;
    max-width: 1000px;
    margin-bottom: 3rem;

    display: flex;
    flex-direction: column;
  }
`;